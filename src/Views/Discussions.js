import React from 'react';
import { Card, Container, CardHeader, CardBody } from 'reactstrap';

const Discussions = () => {
  return (
    <Container style={{maxWidth: '800px'}}>
      <Card>
        <CardHeader>Discussions</CardHeader>
        <CardBody>Bla bla bla</CardBody>     
      </Card>
    </Container>
  );
};

export default Discussions;