{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Start",
            "request": "launch",
            "command": "npm run start",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Build",
            "request": "launch",
            "command": "npm run build",
            "cwd": "${workspaceFolder}"
        },
        {
            "type": "node-terminal",
            "name": "Deploy to Firebase",
            "request": "launch",
            "command": "npm run deploy",
            "cwd": "${workspaceFolder}"
        }
    ]
}